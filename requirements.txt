# ProcurementGuard — pinned. Day 6 deploys from this file; an unpinned
# environment is a deployment that breaks on someone else's machine.
numpy>=1.26
pandas>=2.2
scikit-learn>=1.4
xgboost>=2.0
networkx>=3.2          # Day 5: the graph layer
shap>=0.44             # Day 7: per-alert explanations
streamlit>=1.30        # Day 6: the dashboard
