# Copy this file to .env and fill in your key.
# .env is git-ignored — it will never be committed. This .example file is safe
# to commit: it shows the SHAPE, not the secret.
#
# Optional. Without it, alert briefs are written by a deterministic template
# instead of Claude. Everything else works identically.
ANTHROPIC_API_KEY=your_key_here
